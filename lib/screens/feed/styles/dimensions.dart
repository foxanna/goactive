import 'package:flutter/material.dart';

const activityImageHeight = 200.0;
const userAvatarRadius = 16.0;

const activityDetailsPadding = EdgeInsets.only(
  top: 12.0,
  bottom: 24.0,
  left: 16.0,
  right: 16.0,
);
